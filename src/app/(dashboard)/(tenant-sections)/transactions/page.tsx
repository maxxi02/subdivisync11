import React from "react";

const TransactionSection = () => {
  return (
    <div>
      this part where the tenants pay their own properties monthly or depends on
      their timeline or plan
    </div>
  );
};

export default TransactionSection;
